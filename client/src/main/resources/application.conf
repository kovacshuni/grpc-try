akka {
  loglevel = INFO
  event-handlers = ["akka.event.slf4j.Slf4jEventHandler"]

  grpc.client {
    "grpctry.GreeterService" {
      host = 127.0.0.1
      port = 8080
      use-tls = false
    }
  }
}
